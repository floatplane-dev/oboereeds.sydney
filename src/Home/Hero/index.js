import React, { Component } from "react";
import ReactDOM from "react-dom";


class Hero extends Component {
  render() {
    return (
      <section id="hero">
        <div className="image" />
      </section>
    );
  }
}

export default Hero;
